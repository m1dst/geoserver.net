services:
  geoserver:
    image: docker.osgeo.org/geoserver:2.28.2
    container_name: geoserver-net-test
    ports:
      - "8080:8080"
    environment:
      GEOSERVER_ADMIN_USER: admin
      GEOSERVER_ADMIN_PASSWORD: geoserver
